[toolchain]
channel = "1.93.0"
targets = ["x86_64-pc-windows-msvc"]

# Add `"rust-analyzer.check.command": "clippy", "editor.formatOnSave": true,` to VSCode's settings.json for auto-formatting.
components = ["clippy", "rustfmt"] # Auto-installs mandatory tools.
